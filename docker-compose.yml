version: '3.8'

networks:
  default:

services:
  db:
    image: mysql:8.0.36
    container_name: museum_container
    ports:
      - 3308:3306
    volumes:
      - "./.data/db:/var/lib/mysql"
    environment:
      MYSQL_ROOT_PASSWORD: some_pass
      MYSQL_DATABASE: museum_api